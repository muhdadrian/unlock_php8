<?php
# Arrays are data structures that can store multiple values in a single variable. We can create arrays using square brackets and separating the values with commas, like:

$array = [1, 2, 3];  // An array
echo $array[2];